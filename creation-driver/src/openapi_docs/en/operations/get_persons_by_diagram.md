List persons in a diagram

Returns all person entities for the requested diagram in the requested world.

The current implementation accepts `world_id` and the diagram identifier in the request body even though the route uses `GET`.
