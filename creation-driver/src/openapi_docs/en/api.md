OpenAPI documentation for the current `creation-driver` implementation.

This specification is generated with `utoipa` directly from the Axum handlers and their request/response schemas. Human-facing descriptions are stored in external Markdown files so the API text can be maintained separately from the Rust source.

The current published descriptions are written in English. The generated paths, status codes, and payload shapes reflect the current implementation as closely as possible.
